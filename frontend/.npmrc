# Ensure all optional dependencies (like platform-specific binaries) are installed
include-optional=true
# Support cross-platform builds if needed
supportedArchitectures=linux,win32,darwin
supportedPlatforms=x64,arm64
